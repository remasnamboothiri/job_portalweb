services:
  - type: web
    name: job-portal
    env: python
    runtime: python-3.12.10
    region: oregon
    plan: free
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
      python manage.py migrate
    startCommand: gunicorn job_platform.wsgi:application --bind 0.0.0.0:$PORT --timeout 60 --workers 2 --max-requests 1000 --max-requests-jitter 100
    healthCheckPath: /
    envVars:
      # Database Configuration
      - key: DATABASE_URL
        value: postgresql://job_platform_db4_user:4sa3QUnAJYV5U9zozaZgOJugT31XOolq@dpg-d4g0dtkhg0os73cn804g-a.oregon-postgres.render.com/job_platform_db4
      - key: DB_NAME
        value: job_platform_db4
      - key: DB_USER
        value: job_platform_db4_user
      - key: DB_PASSWORD
        value: 4sa3QUnAJYV5U9zozaZgOJugT31XOolq
      - key: DB_HOST
        value: dpg-d4g0dtkhg0os73cn804g-a
      - key: DB_PORT
        value: 5432
      
      # Django Configuration
      - key: SECRET_KEY
        value: jvaq=3hqcrvdi^sbmorqdi3*2@k2fe@3x-%)z6=k+=w3o+&dm!
      - key: DEBUG
        value: False
      - key: ALLOWED_HOSTS
        value: 127.0.0.1,localhost,job-portal.onrender.com
      
      # Email Configuration (Set these as secrets in Render Dashboard)
      - key: EMAIL_HOST
        value: smtp.gmail.com
      - key: EMAIL_PORT
        value: 587
      - key: EMAIL_HOST_USER
        sync: false
      - key: EMAIL_HOST_PASSWORD
        sync: false
      - key: EMAIL_USE_TLS
        value: True
      - key: DEFAULT_FROM_EMAIL
        sync: false
      
      # Google OAuth Configuration (Set these as secrets in Render Dashboard)
      - key: GOOGLE_CLIENT_ID
        sync: false
      - key: GOOGLE_SECRET_KEY
        sync: false
      
      # NVIDIA API Configuration (Set as secret in Render Dashboard)
      - key: NVIDIA_API_KEY
        sync: false